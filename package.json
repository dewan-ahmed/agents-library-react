{
  "name": "agents-library-react",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "build": "tsc --noEmit && vite build",
    "sync:marketplace": "node scripts/sync-marketplace.mjs && npm run generate:pipelines",
    "generate:pipelines": "node scripts/generate-pipeline-samples.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 3000",
    "start": "vite preview --host 127.0.0.1 --port 3000",
    "validate:catalog": "node scripts/validate-catalog.mjs"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
